{
  "name": "stock-monitor",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"pnpm run dev:backend\" \"pnpm run dev:frontend\"",
    "dev:backend": "cd backend && pnpm run start:dev",
    "dev:frontend": "cd frontend && pnpm dev",
    "install:all": "cd backend && pnpm install && cd ../frontend && pnpm install",
    "format": "cd backend && pnpm run format && cd ../frontend && pnpm run format",
    "lint": "cd backend && pnpm run lint && cd ../frontend && pnpm run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
